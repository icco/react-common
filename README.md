# react-common

A set of common components I use on my sites.

Theme components use [@wrksz/themes](https://themes.wrksz.dev/) (Next.js 16+). Install it alongside this package; `ThemeProvider` is re-exported from `@wrksz/themes/next` (async Server Component — use in the root layout, not inside `"use client"`).
